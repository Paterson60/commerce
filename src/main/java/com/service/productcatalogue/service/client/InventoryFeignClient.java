package com.service.productcatalogue.service.client;

/**
 * FeignClient implementation for communication with inventory catalogue
 *
 */

import com.service.productcatalogue.dto.InventoryDto;
//import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;

//@FeignClient("inventoryCatalogue")
//public interface InventoryFeignClient {

//    @GetMapping(value = "/api/fetchquantity", consumes = "application/json")
//    public ResponseEntity<InventoryDto> fetchQuantity(@RequestParam String sku);


//}
